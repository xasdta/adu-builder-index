{
 "_schema": "Each entry: {slug (must match builder page), website?, phone?, service_area?, claimed_date (YYYY-MM-DD)}. Add an entry only after verifying the claimant: license number matches L&I, email domain matches website (or equivalent proof).",
 "builders": []
}